{
  "name": "nookies-pings",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "bunx wrangler deploy && wrangler tail",
    "worker": "bun run runner/index.ts",
    "setup:cmd": "bun run nookie/commands/index.ts",
    "setup:db": "wrangler d1 migrations apply DB"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "cloudflare": "^5.2.0",
    "wrangler": "^4.60.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "croner": "^9.1.0",
    "discord-interactions": "^4.4.0",
    "discord.js": "^14.25.1",
    "p-queue": "^9.1.0"
  }
}
